Instead of a small NER model, you can use a general-purpose LLM (like a local Mistral or an API like Gemini) to read the first page of the PDF and output a structured JSON.get me code to extract metadata nd toher important details of very perticular case file/judgment and later embbed the datato extract metadata and other important details of very perticular case file/judgment and later embbed the data 50k pdf files, check for any huggiingface models to this/ to extract metadata and other important detailsoption 2 seems good but i wan the meta data links or connection to exist with the text embbeded chunks cause it'll help in faster and more accurate retrevialgive me all the codes to convert those 50k pdf documents as as needs i meantioned, make sure i happens in batch wirse like 500 and 500 so.. i should be able to pause, god knows how long it takes, get all the code related to run thisUse an LLM as an Extractor
Instead of a small NER model, you can use a general-purpose LLM (like a local Mistral or an API like Gemini) to read the first page of the PDF and output a structured JSON.  use like 